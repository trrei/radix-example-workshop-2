apiVersion: radix.equinor.com/v1
kind: RadixApplication
metadata:
  name: echo
spec:
  environments:
    - name: development
      build:
        from: master
  components:
    - name: www
      src: ./www
      ports:
       - name: http
         port: 3000
      public: true
      environmentConfig:
        - environment: development
          monitoring: false
          resources:
            requests:
              memory: "50Mi"
              cpu: "10m"
            limits:
              memory: "54Mi"
              cpu: "20m"
          replicas: 1
    - name: echo
      src: ./echo
      ports:
       - name: http
         port: 3001
      public: false
      environmentConfig:
        - environment: development
          monitoring: true
          resources:
            requests:
              memory: "50Mi"
              cpu: "10m"
            limits:
              memory: "54Mi"
              cpu: "20m"
          replicas: 2
  dnsAppAlias:
    environment: development
    component: www
